machine:
  timezone:
    America/Los_Angeles
    dependencies:
    override:
- mvn --fail-never dependency:go-offline || true
- mvn -o clean install -DskipTests=true

test:
  override:
    - /bin/true
